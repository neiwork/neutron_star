#pragma once

#include <fmath/physics.h>

//void setMatrix(Matrix& bb, Vector& cd, Vector& ad, Vector& bd, double delta_t);

void setMatrix(Matrix& bb, Vector& cd, Vector& ad, Vector& bd, Vector& d, double delta_t, Matrix& s, int k);